# Bounded same-card rendered journey proof

Parent: `ba3ff7c4e0564cfe7347668c52e579ac0e737225`, tree `4e8669bb8d91860943c519587ce03e5807b6d371`.
Dependency remains PR #140 `108c668a3cbea393c0d8427740c74848c95c929e`.

The accepted `gpt-6-astra / ultra` writer adds a no-new-dependency browser proof in the existing Partner Publication Integration Proof workflow. It must use an already-installed Chrome executable. Missing Chrome is a hard failure; there is no browser installer, download, hosted browser, fallback pass, new credential, or package change.

The real claim-return query, Profile, Community Pass, editor and media components render against an explicitly isolated synthetic transport. The fixture owns older canonical A and newer B. Its projection/filter-aware mock preserves request data in its own browser session across reload; it does not represent authentication, server authorization or hosted persistence. Review submission intentionally leaves published values unchanged.

Desktop 1280×900 and mobile-emulated 390×844 scenarios cover exact A routing; preservation of Restaurant + Vendor when adding Events; submitted proposed name/categories across reload; duplicate-review lockout; keyboard-only navigation, modal entry/wrap/Escape/return; named controls and grouped categories; visible focus outline and its contrast against the white sheet; horizontal overflow; failed then retried synthetic upload; exact owner/A storage-path and request targeting; and return to A after same-card refresh. A source-transform negative control removes the real editor focus ref and must fail the specific rendered focus-entry assertion. This is not a full accessibility audit or physical-device/screen-reader proof.

The new focused product repair gives the editor/media dialogs keyboard focus management, inert background, Escape close and opener restoration; their prior `aria-modal` declaration alone did not implement these behaviors. Editor categories use a named fieldset, and the hidden media file input has a name. No catalog, auth, payment, pricing, migration, ownership, offer or publication authority changes.

Artifacts retain checked-out SHA/tree, browser/Node version, dirty-source flag, per-viewport assertions, screenshot hashes and isolation limits. The test server binds a fresh loopback port, disables environment/config loading, aliases away the actual backend client, strips only the external font import, applies a restrictive CSP and rejects any observed non-loopback request. The browser gets a fresh temporary profile and a sanitized environment. It is closed and the profile removed after execution.

Local execution: 70/70 existing focused JavaScript tests, script syntax, repository evidence/ledger checks and ten ledger negative controls passed. Rendered local execution and build were **not run successfully**: local application dependency directories were incomplete and no already-installed Chrome was present. Requests to download Chrome or add a browser library were rejected; neither was retried or replaced with an installer. No dependency metadata changed. Actual rendered acceptance depends on the literal-head automatic CI job and its retained artifacts, not this document.

No hosted account/data/SQL, real email/invitation, approval, publication, merge, manual deployment, production or financial action is authorized or performed. Real verification-email/recovery, hosted journey, physical device/assistive testing, schema/release preflight and explicit human release acceptance remain open. No forecast acceleration is claimed.
